// App.stimulus.connectedControllersByIdentifier('channel').forEach(v => {
//   v.createMessage(<%= @message.text.to_json.html_safe %>);
// });

const channelEl = document.getElementById('channel');

App.stimulus.getControllerForElementAndIdentifier(channelEl, 'channel')
  .createMessage(<%= @message.to_json.html_safe %>);
