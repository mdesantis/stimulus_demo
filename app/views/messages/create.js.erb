// App.stimulus.connectedControllersByIdentifier('chatroom').forEach(v => {
//   v.createMessage(<%= @message.text.to_json.html_safe %>);
// });

const chatroomEl = document.getElementById('chatroom');

App.stimulus.getControllerForElementAndIdentifier(chatroomEl, 'chatroom')
  .createMessage(<%= @message.to_json.html_safe %>);
